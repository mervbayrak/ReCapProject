import { CarSearchPipe } from './car-search.pipe';

describe('CarSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CarSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
